version: '2.0'

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    
  postgres:
    image: postgres
    ports:
      - "5432:3000"
    environment:
      POSTGRES_USER: "soumay"
      POSTGRES_PASSWORD: "xyz"  